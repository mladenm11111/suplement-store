{% extends 'layout.twig' %}

{% block content %}
<h1 class="mb-4">Brendovi</h1>

<a href="{{ base_url }}brand/add" class="btn btn-success mb-3">Dodaj brend</a>

<table class="table table-bordered table-hover">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Naziv</th>
            <th>Opis</th>
            <th colspan="2" class="text-center">Akcije</th>
        </tr>
    </thead>
    <tbody>
        {% for brand in brands %}
        <tr>
            <td>{{ brand.id }}</td>
            <td>{{ brand.name }}</td>
            <td>{{ brand.description }}</td>
            <td>
                <a href="{{ base_url }}brand/update/{{ brand.id }}" class="btn btn-warning btn-sm">Izmeni</a>
            </td>
            <td>
                <form method="POST" action="{{ base_url }}brand/delete/{{ brand.id }}" onsubmit="return confirm('Da li ste sigurni?')">
                    <button type="submit" class="btn btn-danger btn-sm">Obriši</button>
                </form>
            </td>
        </tr>
        {% else %}
            <tr><td colspan="5">Nema brendova.</td></tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}